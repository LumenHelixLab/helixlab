{
  "name": "@helixlab/api-gateway",
  "version": "0.1.0",
  "description": "HelixLAB API Gateway — Express REST + WebSocket, JWT auth, rate limiting, JSON-Schema validation (spec §4.2)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}